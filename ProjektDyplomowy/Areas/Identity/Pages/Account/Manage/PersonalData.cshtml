@page
@model PersonalDataModel
@{
    ViewData["Title"] = "Dane osobowe";
    ViewData["ActivePage"] = ManageNavPages.PersonalData;
}

<h3>@ViewData["Title"]</h3>

<div class="row">
    <div class="col-md-6">
        <p>Twoje konto zawiera dane osobowe które nam dostarczyłeś. Ta strona pozwala Ci na pobranie lub usunięcie tych danych. <span class="text-danger">Usunięcie danych spowoduję także usunięcie konta.</span></p>
        <form id="download-data" asp-page="DownloadPersonalData" method="post" class="form-group">
            <div class="form-group">
                <button type="submit" class="btn btn-primary py-2 px-4 me-2">
                    <span class="mr-3"><i class="fas fa-download"></i></span>
                    Pobierz
                </button>
                <a id="delete" asp-page="DeletePersonalData" class="btn btn-danger py-2 px-4">
                    <span class="mr-3"><i class="far fa-trash-alt"></i></span>
                    Usuń
                </a>
            </div>
        </form>
    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}
